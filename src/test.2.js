var marmota = new Marmota(2);
{ manca(); var iarba_verde = 10; }
var ciocolate = getCiocolate();
function traba() {
  for (var ciocolata in ciocolate) {
    if(foo == bar)
      marmota.inveleste(ciocolata, "staniol")
  }
  fericita(marmota);
}
var f = function(buz) { return buz; }
